# CodBlock 🛡️
Platform eksekusi kode jarak jauh (Remote Code Execution) untuk Termux tanpa perlu install package lokal.

## Cara Install (Client)
1. Salin script `cblock.sh` ke folder bin Termux:
   ```bash
   cp client/cblock.sh $PREFIX/bin/cblock
   chmod +x $PREFIX/bin/cblock
